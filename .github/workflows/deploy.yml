name: Deploy to SiteGround

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ssh.distilldesign.ca
          username: u2094-debsyjvsjdol
          key: ${{ secrets.SITEGROUND_SSH_KEY }}
          port: 18765
          script: |
            cd ~/www/mentalhealthbranding.distilldesign.ca/public_html
            git fetch origin main
            git reset --hard origin/main
